# PROGRESS (update at the end of every session)
Legend: [ ] todo · [~] in progress · [x] done · ✔ verified (how)

## Status snapshot
- Mail platform: **built + e2e verified** (re-run 2026-09-22 on the Docker stack, after Phases 0-6 + Phase 5 follow-up: 5/5 PASS): authenticated SMTP send, sender-spoof rejection, bad-login rejection, IMAP delivery, DKIM signature.
- NOT verified: webmail UI click-through, real DNS/Let's Encrypt, deployment on a real VPS (Mac Docker only, no Linux host in this environment).
- Hosting-panel expansion: **Phase 0 (foundation) built** (2026-09-21) - accounts/roles/2FA/audit/impersonation/tenancy, agent skeleton, cPanel + WHM UI. Phase 1 (websites) built, Phase 2 (file manager + databases) built, see below. Phase 3 (DNS zone editor) built, see below. Phase 4 (backups + cron) built, see below. Phase 5 (API/webhooks/WHMCS/reseller caps/white-label) built, see below. Phase 6 (firewall/fail2ban/WAF/ClamAV/resource graphs/self-update) built, see below.
- Phase 0 verification: typecheck + `next build` pass; 6 unit tests pass; 65-check tenant-isolation E2E passes against the real built app on an in-memory Postgres (pglite) with real Prisma migrations; migration backfill tested on legacy data.
- **2026-09-22: Docker came up on this Mac for the first time this session** (it had been down/corrupted through Phases 1-6). Ran the full local stack (`scripts/dc up -d --build`) and, for the first time, actually verified against it rather than just typecheck/build: migrations `0009_security_ops` and `0010_reseller_branding_and_polish` applied cleanly; `e2e-mail.py` 5/5 PASS; `e2e-tenancy.py` **ALL PASS** (81 checks, including the new malware-scan isolation block written this session); a manual browser-less smoke pass (login as admin/reseller, walk every new page) confirmed `/admin/security`, `/admin/updates`, `/admin/server`, `/admin/api` all render with no server errors, reseller branding saves and immediately shows in the WHM shell nav ("Acme Hosting" replacing "Hosting panel"), and the site-detail page shows the WAF selector + malware-scan section. A manual scan attempt correctly recorded a `scan_runs` row with `status=error, error="Agent not configured"` (no agent in this Mac-only stack) instead of crashing. Still NOT run: anything needing the host agent itself (sites/DB/DNS/backups/firewall/fail2ban/ClamAV/metrics/self-update all need a Linux host with the agent installed, not just Docker) or a real browser (this was all done via scripted HTTP, not a visual check).

## Step 0 — context system
- [x] CLAUDE.md, docs/SCOPE.md, docs/ARCHITECTURE.md, docs/DECISIONS.md, PROGRESS.md
- [x] scripts/dc (local compose wrapper), scripts/e2e-mail.py (regression)
- [x] `scripts/e2e-mail.py` run on the real Docker stack (2026-09-21, after Phase 0-5 code): 5/5 PASS (send, spoof rejected, bad login rejected, IMAP delivery, DKIM). First run exposed a script bug (spoof is rejected at RCPT time -> `SMTPRecipientsRefused`); fixed in the script, server behaviour was correct. Docker only worked after freeing Mac disk (needs >10 GB free; caches cleaned).
- [x] Re-run 2026-09-22 after Phase 0-6 + Phase 5 follow-up code (migrations up to `0010`): `e2e-mail.py` 5/5 PASS and `e2e-tenancy.py` ALL PASS (81 checks) against the freshly-built stack.

## Phase 0 — foundation  [x] built, see verification notes
- [x] Prisma: Account, Package, AuditLog, ApiKey; `account_id` (required FK, RESTRICT) on Domain/Mailbox/Alias; `admin_users` folded into `accounts` (migration `0003_accounts`; mailreader grants untouched - new columns are not granted) ✔ migration applied with real `prisma migrate deploy` on pglite; backfill + RESTRICT tested with legacy rows (3 admins incl. username collisions), fresh DB path tested
- [x] Role-aware sessions (`panel_session` JWT, password-fingerprint revocation), `requireRole()`/`scopeFor()`/`getOwned*()`, TOTP 2FA (+8 recovery codes, replay protection), impersonation (admin->any non-admin, reseller->own users, no nesting, banner, audited), suspend/unsuspend/terminate, audit log ✔ e2e-tenancy.py + `npm test` (RFC 6238 vectors)
- [x] Agent skeleton: `agent/agent.mjs` (zero-dep, bearer secret, whitelist: `agent.ping`, `system.info`) + typed client `web/src/server/agent.ts`; shown on Server status ✔ ran locally on macOS over TCP (auth/unknown-method/bad-params rejected). NOT tested: unix-socket mode, systemd unit, container->host reachability (`host.docker.internal`)
- [x] UI: design tokens + `Tile/Meter/SectionCard/DataTable/Icon`, cPanel home (`/cpanel`: search + tiles + usage sidebar), WHM shell (`/admin`), all existing pages migrated + role-scoped; login is now `/login` (email or username) ✔ pages exercised over HTTP; NOT visually reviewed in a browser
- [x] Package limits enforced now: domains + mailboxes (`assertWithinPackage`); other limits land with their features
- [x] Tenant isolation test: `scripts/e2e-tenancy.py [BASE_URL]` (two users, reseller subtree, replayed foreign action forms, limits, impersonation, suspend, 2FA, audit scoping). Mutation-checked: with `scopeFor` disabled it fails (and a replayed delete really deleted the other tenant's domain).
- Known gaps (deliberate, by phase): suspension only blocks panel login (mail/sites keep running until Phase 5 enforcement); a suspended reseller's customers are not auto-suspended; reseller's own package does not cap what it can hand out; per-mailbox `quota 0` can bypass package disk (disk enforcement = Phase 5); ApiKey has a table but no UI/API yet (Phase 5); reseller white-label (name/logo/domain) not done (Phase 5); login rate limiter still in-memory.
## Phase 1 — websites  [~] core built; deploy path UNTESTED on a real host
- [x] `Site` + `SiteDomain` models (migration `0004_sites`, generated with `prisma migrate diff` from the pre-change schema), env vars sealed with AES-GCM, redirects JSON, `maxSites` enforced (`assertWithinPackage`), terminate account deletes its sites
- [x] Agent v0.2.0 (`agent/sites.mjs`): `site.apply|start|stop|status|logs|delete`. Runtime whitelist: static (nginx), PHP 8.1-8.4 (apache), Node 18/20/22, Python 3.12; one container per site (cap-drop ALL, no-new-privileges, pids limit, memory/CPU from package), files in `/srv/accounts/<account>/<site>` (path-jailed), one Caddy snippet per site + reload via Caddy admin API; snippet rolled back if reload fails
- [x] UI (shared WHM/cPanel pages): sites list/create, detail with start/stop/redeploy, start command, force-HTTPS, extra domains/subdomains/addon domains, redirects, env vars, logs, delete; cPanel tile + nav
- [x] ✔ `node --test agent/test/sites.test.mjs` (validation rejects injection attempts; docker args/Caddy snippet), `npm test`, typecheck, `next build`
- [ ] NOT verified: any real deploy (no Docker/Linux here) - docker run flags, container->Caddy networking (`DOCKER_NETWORK`), Caddy `/load` of the Caddyfile + `import` of snippets, admin API `origins`, file ownership per runtime, TLS issuance
- [x] `scripts/e2e-tenancy.py` sites block (isolation, replayed forms, domain claiming) ✔ RUN on the Docker stack 2026-09-21: found A opening B's site page / file manager returned 500 (uncaught `getOwnedSite` throw) instead of 404 - fixed with `notFound()` in `sites/[id]/page.tsx` and `files/page.tsx`; now 81 checks ALL PASS
- [ ] Not built (still in SCOPE): Docker image/compose runtime, Git deploy, one-click WordPress (needs Phase 2 databases), per-site access logs (only container logs), SSL status display
## Phase 2 — databases, file manager, SFTP/FTP  [~] file manager + databases built; SFTP/FTP NOT started
- [x] Agent v0.3.0: `agent/files.mjs` (`file.list|read|write|mkdir|delete|rename|chmod|zip|unzip`, jailed per site, symlink-safe) and `agent/databases.mjs` (`db.create|setPassword|drop` for MariaDB + PostgreSQL); request body cap raised to 36 MB for uploads (25 MB file limit)
- [x] Web: `Database` model (migration `0005_databases`, generated from the pre-change schema), `maxDatabases` enforced, terminate account drops its databases, file manager page (browse/upload/edit/new file+folder/rename/chmod/zip/unzip/delete) and Databases page (create/show password/reset/delete) in both shells, cPanel tiles + Databases meter, link from site page
- [x] ✔ `node --test agent/test/*.test.mjs` (9 pass: jail traversal + symlink escapes, param validation, SQL builders, rename/zip/unzip round trip); the agent file methods were also smoke-run on macOS against a temp `SITES_ROOT` (found + fixed a rename bug); `npm test`, typecheck, `next build` pass
- [ ] NOT verified: any real DB create/drop (no MariaDB/PostgreSQL server here; `mariadb`/`psql` CLI invocations, `'%'` host grant, PG `WITH (FORCE)` needs PG13+), chown to runtime uid (needs root/Linux), zip/unzip on the VPS, browser click-through, `e2e-tenancy.py` (new files+databases block WRITTEN, NOT RUN; Docker unavailable), `e2e-mail.py`
- [ ] Not built (still in SCOPE): SFTP (chrooted virtual users), FTP (TLS only), phpMyAdmin/Adminer SSO, per-DB size display, remote-access allow-list, DB user separate from DB, file download/move/copy, multi-select, disk quota enforcement
## Phase 3 — PowerDNS  [~] zone editor + DNSSEC built; NOT run against a real PowerDNS
- [x] Agent v0.4.0 `agent/dns.mjs`: `dns.zoneCreate|zoneDelete|zoneGet|recordAdd|recordDelete|dnssec` over the PowerDNS HTTP API; pure validators (record names jailed to the zone, per-type content builders for A/AAAA/CNAME/MX/TXT/SRV/CAA/NS, TXT chunking, TTL bounds, apex NS/CNAME/SOA protected) (DECISIONS #19)
- [x] Web: `DnsZone` model (migration `0006_dns`, hand-written like 0004/0005: no shadow DB here), `server/dns.ts`, `admin/dns-actions.ts`, zone list + zone detail (add/delete records, DNSSEC enable/disable + DS) in both shells, cPanel tile + nav, account terminate deletes zones, starter template (A/www + mail records when the same account has that mail domain)
- [x] ✔ `node --test agent/test/*.test.mjs` (12 pass, 3 new DNS tests), `npm test`, `tsc --noEmit`, `next build` pass; `prisma validate` ok
- [ ] NOT verified: any call to a real PowerDNS (no pdns here): payload shapes for zone create (`nameservers` + follow-up PATCH), rrset REPLACE/DELETE, cryptokey POST (`algorithm: "ecdsap256sha256"`) and the `ds` field, `?rrsets=true&rrset_name=` filter; pdns.conf from DECISIONS #19; browser click-through; migration 0006 applied; `e2e-tenancy.py` has NO zone block yet (needs an agent/PowerDNS to create zones, or a stub)
- [ ] Not built (still in SCOPE): DNS templates as an admin-editable feature (only a fixed starter set), Cloudflare as a zone provider, edit-in-place (delete + add instead), zone import/export, secondary/AXFR, PowerDNS install/compose service, resolvers/glue guidance
## Phase 4 — backups + cron  [~] built; NOT run against real restic/Docker
- [x] Agent v0.5.0: `agent/cron.mjs` (`cron.run`: docker exec in the site container as its uid, `timeout -s KILL`, output cap, account-label check) and `agent/backup.mjs` (`backup.run|list|restore|deleteSnapshot|purgeAccount`; restic repo per account; site files, DB dumps via stdin, Maildir; retention; restore verifies host/tag/path) (DECISIONS #20, #21)
- [x] Web: models `CronJob`, `CronRun`, `BackupPolicy`, `BackupRun` (migration `0007_cron_backups`, generated with `prisma migrate diff` from the pre-change schema), `lib/cron.ts` + `lib/backup-schedule.ts` (pure), `server/cron.ts`, `server/backups.ts`, minute ticker in `jobs.ts`, pages `cron` + `backups` in both shells (schedule/retention form, back up now, runs, snapshots with restore/delete), cPanel tiles + cron meter, `maxCronJobs` enforced, account terminate purges the backup repo
- [x] ✔ `node --test agent/test/*.test.mjs` (18 pass; incl. backup.run flow against a stub restic), `npm test` (cron parser + backup slots), `tsc --noEmit`, `next build`
- [ ] NOT verified: real restic (init/backup/forget/restore/dump flag behavior, `--one-file-system`, `summary.total_bytes_processed` needs restic >= 0.17 else size shows "-"), mariadb-dump/pg_dump/restore pipes, real `docker exec` + `timeout` inside each runtime image, migration 0007 applied, browser click-through, ticker timing, `e2e-tenancy.py` (no cron/backup block yet), `e2e-mail.py`
- [ ] Not built (still in SCOPE): off-site backup targets (S3/SFTP), per-item restore of single files, backup of Docker image/compose sites, cron output e-mail, a cron/backup disk quota

## Phase 5 — reseller caps, REST API, webhooks, WHMCS module, white-label  [~] built; NOT run against a live stack
- [x] Reseller cap: a reseller's packages may not exceed its own package (`packageOverruns`, 0 = unlimited counts as exceeding a capped field); enforced on package create/update
- [x] `Webhook` model (migration `0008_webhooks`, hand-written); `server/webhooks.ts`: HMAC-SHA256 signed POST (`X-Webhook-Signature`/`-Timestamp`), https only, private-IP refusal at save AND at delivery (DNS re-resolved), no redirects, 8 s timeout; events `account.created|suspended|unsuspended|terminated|package_changed` go to the webhooks of every ancestor of the account
- [x] API keys (`shk_...`, only sha256 stored, shown once via sealed flash), page `/admin/api` (keys + webhooks) for admin/reseller; `lib/apikey.ts` (only active admin/reseller may use a key)
- [x] REST `/api/v1`: `GET packages`, `GET|POST accounts`, `GET|DELETE accounts/{username}`, `POST accounts/{username}/suspend|unsuspend|password|package`; scoped to the key owner's subtree (never self/admins), audited (`via: api`), failed-auth rate limit; suspend/terminate logic shared with the UI in `server/accounts.ts`
- [x] WHMCS module `integrations/whmcs/modules/servers/shivapphub/shivapphub.php` (Create/Suspend/Unsuspend/Terminate/ChangePassword/ChangePackage/TestConnection; server Password field = API key)
- [x] API key scopes + expiry (migration `0010_reseller_branding_and_polish`): `ApiKey.scope` (full/read, read enforced as GET-only in `/api/v1`), `ApiKey.expiresAt` (30/90/365 days or never, checked in `authenticateKey`); UI in `/admin/api`
- [x] Webhook retries + delivery log: `WebhookDelivery` (one row per attempt), 4 retries with backoff (1m/5m/30m/2h, `BACKOFF_SEC`), a `pending->sending` claim so a crashed process can't double-deliver (stale `sending` rows older than 2 min are requeued), last 20 deliveries kept per webhook, retry sweep on the existing minute ticker (`processDueWebhookDeliveries`); delivery log shown per webhook in `/admin/api`
- [x] Reseller white-label: `Account.brandName`/`brandLogoUrl` shown instead of "Hosting panel" in the reseller's own WHM shell and its users' cPanel shell (`lib/brand.ts`, resolved from the session - no host lookup needed), settings form on the shared "My account" page (reseller role only). `brandDomain` is stored + validated (unique, bare hostname) but **not served**: making it work needs the request's `Host` header resolved to an account before any auth check runs, and this app's Edge middleware can't reach Postgres to do that (see DECISIONS #24 - `runtime: "nodejs"` on middleware silently disabled it entirely here, confirmed by an empty `middleware-manifest.json`, so it was reverted) - deferred rather than shipped half-working
- [x] ✔ `tsc --noEmit`, `next build`, and the existing `npm test` (21 pass) + agent suite (39 pass) still pass unmodified
- [x] 2026-09-22: migrations `0008` and `0010` confirmed applied on the real Docker stack; `/admin/api` and the branding form on `/admin/account` confirmed rendering and saving correctly via a scripted HTTP smoke pass (reseller branding immediately showed in the WHM shell nav after saving)
- [ ] NOT verified: any real API call over HTTP with an actual Bearer key (incl. the new scope/expiry checks - only the UI forms were smoke-tested, not `/api/v1` itself), webhook retry delivery to a real receiver, the PHP module (no php here, not even `php -l`; never run inside WHMCS), `e2e-tenancy.py` has NO API block yet (key scoping is untested end to end), a real browser (the smoke pass was scripted HTTP, not a visual check). No automated tests were added for the webhook retry/claim logic or the API scope/expiry checks (this repo has no harness that mocks `fetch`/Prisma at that level) - reasoned through by hand instead of tested; worth a real test pass before relying on it.
- [ ] Not built (still in SCOPE, deferred): reseller custom domain routing (see above), enforcement of suspension on mail/sites, disk/CPU/RAM/bandwidth enforcement, WHMCS client-area single sign-on, usage sync

## Phase 6 — security hardening, monitoring, self-update  [~] built; NOT run against real nft/fail2ban/ClamAV/restic-adjacent host tools
- [x] Agent v0.6.0: `agent/firewall.mjs` (nftables, one `inet mailhost` table, atomic replace, check-before-load-before-persist, prerouting blocks before Docker DNAT), `agent/fail2ban.mjs` (status/ban/unban via `fail2ban-client`; jails themselves configured by `deploy/fail2ban/*`, not the panel), `agent/clamav.mjs` (`clamscan` one site's jail, report-only), `agent/metrics.mjs` (raw `/proc` counters), `agent/update.mjs` + `agent/update-run.mjs` (self-update runner, `systemd-run`/detached) (DECISIONS #23)
- [x] Web: models `FirewallRule`, `ScanRun`, `MetricSample`, `Site.waf` (migration `0009_security_ops`, hand-written), `lib/security-core.ts` (pure: CIDR/port parsing, lock-out guard, metric sampling, downsampling, nightly-due), `server/security.ts`, `app/admin/security-actions.ts`; pages `/admin/security` (firewall rules + fail2ban, admin only), `/admin/updates` (self-update, admin only), resource-usage sparklines added to `/admin/server`; malware scan (manual + nightly) added to the site detail page (shared, tenant-scoped) with a WAF selector in site settings; nightly ClamAV sweep + once-a-minute metrics collector wired into `jobs.ts`
- [x] ✔ `node --test agent/test/*.test.mjs` (39 pass, incl. `deploy.test.mjs` for the fail2ban filter regex and firewall check-then-load sequencing), `node --test web/tests/*.test.ts` (21 pass, incl. `phase6.test.ts`), `npx tsc --noEmit`, `npx next build` all pass
- [x] 2026-09-22: migration `0009` confirmed applied on the real Docker stack; `e2e-tenancy.py`'s new malware-scan isolation block (B scans own site; A replaying B's scan-now form is refused) run for real - **both PASS**; `/admin/security`, `/admin/updates`, `/admin/server` and a site's WAF selector + malware-scan section all confirmed rendering via a scripted HTTP smoke pass; a manual scan attempt correctly wrote a `scan_runs` row with `status=error, error="Agent not configured"` instead of crashing (no agent in this Mac-only stack)
- [ ] NOT verified: any real `nft`/`fail2ban-client`/`clamscan`/`systemd-run` call (no Linux host here - Docker alone doesn't run the agent), the Coraza-built Caddy image (`caddy/Dockerfile` untested), a real self-update run (git fetch/merge/compose build/restart), metrics collection running for real (needs the agent), a real browser (the smoke pass was scripted HTTP, not a visual check). Firewall/fail2ban/update pages are admin-only system-scope, not per-tenant, so no scoping block was added for them.
- [ ] Not built (still in SCOPE, deferred): resource-graph alerting/thresholds, a dedicated cross-account "infected sites" view (today only visible per-site), fail2ban jail editing from the panel (jails are files, by design - see DECISIONS #23), update rollback UI (the agent restores the previous commit on a failed build automatically, but there's no "roll back a working-but-unwanted update" button)

## Known bugs / notes
- Alias forwarding to external addresses has no SRS.
- Login rate limiter is in-memory (single web instance).
- Dev Docker Desktop needed a purge once; test agent/site features on Linux.
- 2026-09-22: two-part UI polish, no scope/data changes. (1) `web/src/app/globals.css` palette: checked first - no mock/placeholder data anywhere in `web/src` (every page/tile/stat is Prisma- or agent-backed, per the SCOPE rule); repainted the shared palette away from the default-AI-template look (muted steel-blue brand instead of Tailwind-blue-600, fixed dark-navy WHM sidebar via new `--side-*` tokens instead of a white sidebar on gray, brand-colored underline on the cPanel top bar, neutral tile hover, 10px radius). (2) cPanel-authenticity pass after feedback that it still didn't look like real cPanel/WHM: added a 7-color category palette (`.c-blue/amber/violet/teal/green/red/slate`, each sets `--tile-c`), gave every WHM sidebar item and cPanel top-nav item its own colored icon (`NavLink` gained `icon`/`color` props, `client.tsx`), and turned the cPanel home tiles into circular colored icon badges (`.tile-icon` 46px circle with a `color-mix()` tint, tile card border/shadow removed so icon+label sit directly on the page like real cPanel). ✔ `tsc --noEmit` + `next build` pass; stack rebuilt in Docker and verified for real this time (scripted login via the server-action form, fetched `/admin` HTML - all 7 color classes + 21 `nav-icon` spans present, confirmed compiled CSS has the `color-mix` circle rule); `e2e-mail.py` 5/5 PASS after the rebuild. NOT done: an actual visual/browser check (no browser tool in this environment) - only HTML/CSS presence was verified, not how it actually looks; ask the user to eyeball `http://localhost:3100/admin` and `/cpanel`.
- 2026-09-22: added `marketing/` - a standalone static landing page for `shivdomains.in` (plain HTML/CSS, no framework, no auth, not part of the Next.js app), per user request for a light-theme "simple clear" landing site. Brand name on the page is "ShivDomains" (inferred from the domain - not confirmed by the user, flag if wrong). Content is honest: real feature list matching what's actually built (sites/mail/databases/DNS/backups/security), no fabricated stats, testimonials or pricing; CTAs are `mailto:hello@shivdomains.in` since there's no public self-signup flow (accounts are admin-provisioned) - **that mailbox does not exist yet**, create it once a mail domain for shivdomains.in is set up in the panel, or swap the address. Wired for deployment: `marketing/caddy/shivdomains.caddy` (hardcoded real domain, not env-var-gated - deliberately, see DECISIONS if this needs revisiting for multi-install reuse) imported by the root `Caddyfile` via a new `import /etc/caddy/marketing/*.caddy` line, mounted in `docker-compose.yml`. Added to `docs/SCOPE.md`. ✔ served both files locally (200/200), div/section tags balanced. NOT verified: real deploy (Caddy will only pick this up on the VPS - DNS for shivdomains.in must point here first), no browser visual check (same limitation as above).
- 2026-09-22 (same day, follow-up): grew `marketing/` from one page to four (`index.html`, `about.html`, `plans.html`, `contact.html`, shared `styles.css`) - user asked to make the landing page more attractive and, if possible, split it into more pages. Added a subtle dot-grid hero background, a 3px brand top bar, hover-lift transitions on buttons/cards, an inline SVG favicon (no external asset), consistent nav+footer duplicated across all four pages (no templating, plain static HTML by design). `about.html` covers the product/philosophy honestly (what it's built on: Postfix/Dovecot/Rspamd/Caddy/PostgreSQL) without inventing a founder bio; `plans.html` has three tiers (Starter/Growth/Reseller) mapped to real, already-shipped features only, deliberately with no invented $ prices - copy says "get in touch for a quote" instead; `contact.html` is just the honest `mailto:` CTA with what to include. ✔ all 4 pages + styles.css served locally (200s), div/section/svg tags balanced per file, CSS brace-balanced (91/91). NOT done: browser visual check (still no browser tool available this session).

## Next 3 actions
~~0. Free disk, restart Docker, `scripts/dc up -d --build`, run `e2e-mail.py` + `e2e-tenancy.py`~~ - **done 2026-09-22**: Docker came up on the dev Mac, migrations through `0010` applied cleanly, both suites pass (see Status snapshot). Docker on this Mac has been unreliable across the project's life, so re-confirm it's still up before relying on it again.
0. Phase 5: add an API block to `e2e-tenancy.py` (reseller key can't touch another reseller's users or admins; revoked/suspended-owner key gets 401; a `scope: read` key gets 403 on POST/DELETE; an expired key gets 401), `php -l` + a test WHMCS install.
0b. Phase 6: build `caddy/Dockerfile` and deploy one site with `waf: detect` to confirm the Coraza snippet actually loads, install nftables/fail2ban/clamav on a Linux VM and exercise `/admin/security` end to end (open a port, block then unblock an address without locking yourself out, ban/unban via a jail), then a real self-update run (needs a git remote + `UPDATE_DIR` checkout on that VM).
1. All of the below still need a **Linux host with the agent installed** - Docker alone (even working, as it now is on the Mac) does not exercise sites/DBs/DNS/backups/cron/firewall/fail2ban/ClamAV/metrics/self-update, since those all go through `agent/*.mjs` which nothing in `docker-compose.yml` runs. This is the single biggest remaining gap.
2. Phase 2: on a Linux VM install MariaDB + PostgreSQL, set agent env (see DECISIONS #18), create one DB per engine, upload/edit/zip files in a site. Then SFTP/FTP.
3. Phase 3: on a Linux VM install pdns + gpgsql, configure per DECISIONS #19, create a zone, `dig @server` records, enable DNSSEC. Then Phase 4 check: install restic, set BACKUP_ROOT/RESTIC_PASSWORD, run a backup + restore of one site, one DB, one mail domain; create a cron job and check history.
3b. Phase 1: on a Linux VM run the agent with SITES_ROOT/CADDY_SITES_DIR/CADDYFILE_PATH/DOCKER_NETWORK, create one site per runtime, check HTTPS + logs + redirects; run `e2e-tenancy.py`. Then Phase 3 (PowerDNS).
4. Verify agent from the container on Linux (`AGENT_URL=http://host.docker.internal:7701`, `AGENT_LISTEN=<bridge ip>:7701`) and the unix-socket mode.
