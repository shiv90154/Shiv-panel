hosts = postgres
user = mailreader
password = ${MAIL_DB_PASSWORD}
dbname = ${POSTGRES_DB}
query = SELECT array_to_string(a.destinations, ',') FROM aliases a JOIN domains d ON d.id = a.domain_id WHERE a.source = '%s' AND a.active AND d.active
